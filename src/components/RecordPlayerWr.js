import React from 'react';
import '../assets/styles/style.css';

const RecordPlayerWr = ({children}) => {
    return (
        <div className='record-player-wr'>
            {children}
        </div>
    )
};

export default RecordPlayerWr;
